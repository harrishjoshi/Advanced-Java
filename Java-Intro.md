# Java Core Fundamentals

---

### Java Basics & Setup

#### Introduction & Environment

* What is Java and why learn it?
* JDK vs JRE vs JVM explained
* Installing Java and setting up a simple IDE (VS Code / IntelliJ)
* Writing and running your first **Hello World** program
* Understanding the basic structure of a Java program (class, `main` method)

#### Variables & Data Types

* Primitive data types: `byte`, `short`, `int`, `long`, `float`, `double`, `char`, `boolean`
* Variable declaration and initialization
* Type casting and conversion
* Constants using the `final` keyword
* **Practice:** Create a program using different data types

#### Operators & Expressions

* Arithmetic operators (`+`, `-`, `*`, `/`, `%`)
* Comparison operators (`==`, `!=`, `<`, `>`, `<=`, `>=`)
* Logical operators (`&&`, `||`, `!`)
* Assignment operators
* **Practice:** Calculation and expression exercises

---

### Control Flow

#### Conditional Statements

* `if`, `else if`, `else` statements
* `switch` statements
* Ternary operator
* **Practice:** Simple grade calculator

#### Introduction to Loops

* `for` loop basics
* `while` loop basics
* Simple iteration examples
* **Practice:** Print numbers 1–10 and calculate the sum

---

## Data Structures & Methods

### Arrays & Strings

#### Arrays

* What are arrays and why use them?
* Declaring and initializing arrays
* Accessing array elements
* Array `length` property
* Looping through arrays (`for` and enhanced `for` loop)
* Common array operations (finding max, sum, average)
* **Practice:** Work with an array of student scores

#### Strings

* String basics and immutability
* Common `String` methods: `length()`, `charAt()`, `substring()`, `indexOf()`
* String comparison (`equals()` vs `==`)
* String concatenation
* **Practice:** String manipulation exercises

---

### Methods & OOP Preview

#### Methods

* Why use methods? (code reusability, organization)
* Method syntax: return type, parameters, method name
* Calling methods
* Return values vs `void` methods
* Method overloading basics
* **Practice:** Create utility methods (calculator functions)

#### Introduction to OOP

* Brief overview of classes and objects
* What makes Java object-oriented?
* Creating a simple class with fields and methods
* Instantiating objects
* **Practice:** Create a simple `Student` or `Car` class
